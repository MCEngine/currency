plugins {
    id 'java'
}

def spigotApiVersion = project.findProperty('spigotmc-api-version')

dependencies {
    compileOnly project(":mcengine-currency-api")
    compileOnly "org.spigotmc:spigot-api:${spigotApiVersion}"
}